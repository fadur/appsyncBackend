type Todo {
  id: ID!
  title: String!
  completed: Boolean!
  priority: Int!
}

type Query {
  getTodos: [Todo]
}

type Mutation {
  createTodo(title: String!, completed: Boolean!, priority: Int!): Todo
}
